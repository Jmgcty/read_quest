import 'package:flutter/material.dart';

class ReaderMoreSettingsScreen extends StatelessWidget {
  const ReaderMoreSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('More Settings Screen'));
  }
}
