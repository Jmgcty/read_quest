import 'package:flutter/material.dart';

class ReaderBookScreen extends StatelessWidget {
  const ReaderBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Book Screen'));
  }
}
