import 'package:flutter/material.dart';

class ReaderGameScreen extends StatelessWidget {
  const ReaderGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Game Screen'));
  }
}
